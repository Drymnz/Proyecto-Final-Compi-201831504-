import { CodeFileDirective } from './code-file.directive';

describe('CodeFileDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeFileDirective();
    expect(directive).toBeTruthy();
  });
});
